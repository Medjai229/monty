#include "monty.h"
/**
 * s_stack - switch to a stack
 * @head: pointer of the stack head
 * @counter: line number
 */
void s_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
