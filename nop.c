#include "monty.h"
/**
 * s_nop - doesn’t do anything
 * @head: pointer of the stack head
 * @counter: line number
 */
void s_nop(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
}
